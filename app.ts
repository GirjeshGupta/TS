// valid javsceipt is valide for typescript

// Arrays

let arr:number[] = [1,2,3,4,56,6];

// Truples 

let arr1 : [string , number] = ["prem",23];

// truple is use for fix size and type assign

// Enum -> enumerations

enum UserRoles {

    ADMIN =  "admin",
    SUPER_ADMIN = "super_admin",
    GUEST = "guest"
}

enum StatusCode {

    ABANDAND = 500,
    NOTFOUND = 404
}

console.log(UserRoles.SUPER_ADMIN);
console.log(StatusCode.ABANDAND);



